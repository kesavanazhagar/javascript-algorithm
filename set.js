/* set is like a array but is compare to array is access and delete the is fester. */
/* Dublicate value is not add in the set */
/* other wish compare the set is equal to array. time complexity same like array. */


const set = new Set([1,2,3]);

set.add(4);             //  the add key word used to add anther value in set.
set.has(4);             //  check the  same will be ready is place the set.
set.delete(2);          //  Delete the specific value will be deleted.
set.clear() ;           //  remove the all the set value.